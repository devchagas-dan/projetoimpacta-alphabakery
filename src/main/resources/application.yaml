server:
  port: 8080

spring:
  application:
    name: AlphaBakery
  datasource:
    url: jdbc:mysql://localhost:3306/alphabakery
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

