import { Box } from "@mui/material"

export default function Header() {
  return (
    <Box
      sx={{
        backgroundImage: "url('/estoque-final.jpg')",
        backgroundSize: "cover",
        height: 700,
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center",
        color: "white"
      }}
    >
      
    </Box>
  )
}